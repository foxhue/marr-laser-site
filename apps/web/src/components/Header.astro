---
import type { SanityImage } from '@foxhue/shared';
import { sanityImageUrl } from '@lib/image';

interface Props {
  siteName: string;
  logo?: SanityImage;
}

const { siteName, logo } = Astro.props;

const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Treatments', href: '/services' },
  { label: 'Pricing', href: '/pricing' },
  { label: 'About', href: '/about' },
  { label: 'Team', href: '/team' },
  { label: 'Contact', href: '/contact' },
];

const currentPath = Astro.url.pathname;
---

<header class="site-header">
  <div class="header-inner container">
    <div class="header-top">
      <a href="/" class="logo-link" aria-label={`${siteName} - Home`}>
        {logo ? (
          <img
            src={sanityImageUrl(logo, 200, 60)}
            alt={siteName}
            class="logo-image"
            width="200"
            height="60"
          />
        ) : (
          <span class="logo-text">{siteName}</span>
        )}
      </a>
    </div>

    <input type="checkbox" id="nav-toggle" class="nav-checkbox sr-only" aria-hidden="true" />
    <label for="nav-toggle" class="nav-toggle" aria-label="Toggle navigation menu">
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </label>

    <nav class="main-nav" aria-label="Main navigation">
      <ul class="nav-list" role="list">
        {navItems.map((item) => (
          <li class="nav-item">
            <a
              href={item.href}
              class:list={['nav-link', { 'nav-link--active': currentPath === item.href }]}
              aria-current={currentPath === item.href ? 'page' : undefined}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background-color: rgba(255, 255, 255, 0.97);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--color-accent);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    min-height: 4.5rem;
  }

  /* Logo — centered on desktop */
  .header-top {
    flex-shrink: 0;
  }

  .logo-link {
    text-decoration: none;
    display: flex;
    align-items: center;
  }

  .logo-image {
    height: 2.5rem;
    width: auto;
  }

  .logo-text {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: 500;
    color: var(--color-neutral-900);
    letter-spacing: 0.02em;
  }

  /* Checkbox (visually hidden, drives menu state) */
  .nav-checkbox {
    position: absolute;
  }

  /* Mobile hamburger toggle */
  .nav-toggle {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 2.5rem;
    height: 2.5rem;
    padding: var(--space-2);
    cursor: pointer;
    border-radius: var(--radius-sm);
    transition: background-color var(--transition-fast);
    order: 1;
  }

  .nav-toggle:hover {
    background-color: var(--color-accent);
  }

  .hamburger-line {
    display: block;
    width: 100%;
    height: 1.5px;
    background-color: var(--color-neutral-700);
    border-radius: 1px;
    transition: transform var(--transition-base), opacity var(--transition-base);
    transform-origin: center;
  }

  /* Navigation panel */
  .main-nav {
    display: none;
    flex-basis: 100%;
    background-color: var(--color-white);
    border-top: 1px solid var(--color-accent);
    order: 2;
  }

  .nav-list {
    display: flex;
    flex-direction: column;
    padding: var(--space-4) 0;
  }

  .nav-link {
    display: block;
    padding: var(--space-3) var(--space-4);
    font-family: var(--font-body);
    font-size: var(--font-size-sm);
    font-weight: 400;
    color: var(--color-neutral-600);
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    border-radius: var(--radius-full);
    transition: color var(--transition-fast), background-color var(--transition-fast);
  }

  .nav-link:hover {
    color: var(--color-primary);
    background-color: var(--color-accent);
  }

  .nav-link--active {
    color: var(--color-primary);
    background-color: var(--color-accent);
    font-weight: 500;
  }

  /* Checkbox hack: show nav when checked */
  .nav-checkbox:checked ~ .main-nav {
    display: block;
  }

  /* Hamburger animation when open */
  .nav-checkbox:checked ~ .nav-toggle .hamburger-line:nth-child(1) {
    transform: translateY(7px) rotate(45deg);
  }

  .nav-checkbox:checked ~ .nav-toggle .hamburger-line:nth-child(2) {
    opacity: 0;
  }

  .nav-checkbox:checked ~ .nav-toggle .hamburger-line:nth-child(3) {
    transform: translateY(-7px) rotate(-45deg);
  }

  /* Desktop layout — centered logo above nav */
  @media (min-width: 768px) {
    .header-inner {
      flex-direction: column;
      flex-wrap: nowrap;
      gap: var(--space-2);
      padding-block: var(--space-3);
    }

    .nav-toggle {
      display: none;
    }

    .nav-checkbox {
      display: none;
    }

    .main-nav {
      display: block;
      flex-basis: auto;
      background: none;
      border: none;
      order: 1;
    }

    .nav-list {
      flex-direction: row;
      justify-content: center;
      gap: var(--space-1);
      padding: 0;
    }

    .nav-link {
      padding: var(--space-2) var(--space-4);
    }
  }
</style>
